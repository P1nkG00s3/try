plugins {
    id 'java'
    id 'pmd'
}


repositories {
    mavenCentral()
}

dependencies {
    implementation files(fileTree(dir: 'jar_files', includes: ['*.jar']))
}

pmd {
    toolVersion = '6.39.0'
    ruleSetFiles = files("pmd.xml")
    ruleSets = []
    ignoreFailures = true
}
